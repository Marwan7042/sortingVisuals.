/*
    sorts.h
    -------
    This header declares the sorting algorithms used in the project.
    Each function sorts an array of integers.
*/

void selection_sort(int array[], int n);
void bubble_sort(int array[], int n);
void merge_sort(int a[], int low, int high);
